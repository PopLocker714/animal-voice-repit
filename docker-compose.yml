services:
  web:
    build: .
    image: animal-voce-app-2
    container_name: animal-voce-app-2
    ports:
      - "8082:3000"
    restart: unless-stopped
    volumes:
      # Persist the SQLite DB and recordings across redeploys.
      - app-data:/app/data

volumes:
  app-data:
